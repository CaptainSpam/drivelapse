<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	package="net.exclaimindustries.drivelapse" android:versionCode="1"
	android:versionName="0.4" android:installLocation="auto">
	<application android:icon="@drawable/icon" android:label="@string/app_name"
		android:debuggable="true">
		<!--
			The camera part MUST always be horizontal for driver reasons. Plus
			the fact that the pictures are always going to be
			horizontally-oriented anyway if we're putting them into a movie
			later. Thus, configChanges always has to include the keyboard hiding,
			the screen's orientation, and the screen's layout.
		-->
		<activity android:label="@string/app_name" android:name=".DriveLapse"
			android:screenOrientation="landscape" android:configChanges="keyboardHidden|orientation|screenLayout"
			android:theme="@android:style/Theme.NoTitleBar.Fullscreen">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>

	<service android:name="AssemblyLine">
</service>
</application>
	<uses-sdk android:minSdkVersion="6" android:targetSdkVersion="6" />

	<uses-permission android:name="android.permission.CAMERA"></uses-permission>
	<uses-permission android:name="android.permission.INTERNET"></uses-permission>
	<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"></uses-permission>
	<uses-permission android:name="android.permission.WAKE_LOCK"></uses-permission>
	<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>
	<uses-feature android:name="android.hardware.camera" android:required="true"/>
	<uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>
	<uses-feature android:name="android.hardware.camera.flash" android:required="false"/>
</manifest> 